import React from 'react'

const Loading = () => {
  return (
    <div style={{ height: "80vh", width: '100%', display: 'flex', justifyContent: 'center', alignItems: 'center', gridRow: 'span 2', gridColumn: 'span 2'}}>
      Loading ...
    </div>
  )
}

export default Loading
